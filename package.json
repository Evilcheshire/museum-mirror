{
  "name": "museum-mirror",
  "version": "0.1.0",
  "private": true,
  "description": "Музейне дзеркало — AR примірочна в браузері",
  "scripts": {
    "dev": "serve . -p 3000 --no-clipboard",
    "lan": "ws --https --port 3000 --hostname 0.0.0.0",
    "tunnel": "cloudflared tunnel --url http://localhost:3000"
  },
  "devDependencies": {
    "local-web-server": "^5.3.0",
    "serve": "^14.2.4"
  }
}
